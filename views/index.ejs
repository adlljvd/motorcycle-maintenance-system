<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - DeFLex Garage</title>
    <!-- Tailwind CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <!-- Import Poppins Font -->
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900&display=swap');

        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f3f4f6; /* Light Gray */
        }

        h1 {
            background-image: linear-gradient(to right, #6366F1, #60A5FA); /* Indigo to Blue Gradient */
            color: white;
            padding: 20px;
            border-radius: 0.5rem;
        }

        .card {
            border: 1px solid #E5E7EB; /* Light border */
            border-radius: 0.75rem; /* Rounded corners */
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 1.5rem;
            margin-bottom: 1.5rem;
        }

        .card-header {
            background-color: #E5E7EB; /* Light gray background for headers */
            padding: 0.75rem 1rem;
            font-weight: 600;
            border-radius: 0.5rem 0.5rem 0 0;
        }

        .card-body {
            padding: 1rem 1rem;
        }

        .card-body p {
            margin: 0.5rem 0;
        }

        .card-img {
            width: 100%;
            height: auto;
            border-radius: 0.5rem;
            margin-bottom: 1rem;
        }

    </style>
</head>
<%- include('./partials/navbar.ejs') %>
<body class="bg-gray-100">
    <div class="container mx-auto py-12 px-4">
        <h1 class="text-4xl font-bold text-center mb-6 shadow-lg rounded-lg">Welcome to DeFLex Garage!</h1>
        <h2 class="text-xl font-semibold text-center text-gray-600 mb-8">Expert Motorcycle Services You Can Trust</h2>

        <div class="flex justify-center">
            <div class="w-full max-w-3xl">
                <% serviceList.forEach(el => { %>
                <div class="card">
                    <!-- Display the service image -->
                    <img src="<%= el.imageURL %>" alt="<%= el.serviceName %>" class="card-img">

                    <div class="card-header">
                        <%= el.serviceName %>
                    </div>
                    <div class="card-body">
                        <p><strong>Description:</strong> <%= el.description %></p>
                        <p><strong>Price:</strong> <%= formatRupiah(el.price) %></p>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>

</html>
